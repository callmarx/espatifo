# NOTE: execute sidekiq with 'dotenv bundle exec sidekiq' to load environment variables

:concurrency: <%= ENV['WEB_CONCURRENCY'] %>
:queues:
  - <%= ENV['SIDEKIQ_QUEUE_PREFIX'] %>_<%= ENV['RAILS_ENV'] %>_default
